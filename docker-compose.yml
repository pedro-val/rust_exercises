version: '3'
services:
  postgres:
    image: postgres:15.3-alpine
    environment:
      POSTGRES_USER: loco
      POSTGRES_DB: myapp_development
      POSTGRES_PASSWORD: loco
    ports:
      - "5432:5432"